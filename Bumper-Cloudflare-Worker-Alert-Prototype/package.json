{
  "name": "bumper-api",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "vitest run && powershell -NoProfile -ExecutionPolicy Bypass -File tests/receive-demo-triggers.test.ps1",
    "typecheck": "wrangler types && tsc --noEmit",
    "cf:typegen": "wrangler types",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.4",
    "@types/node": "^25.7.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.6",
    "wrangler": "^4.90.1"
  }
}
